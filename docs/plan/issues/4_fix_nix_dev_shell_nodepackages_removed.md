# GitHub Issue #4: fix(nix): dev shell fails to evaluate — nodePackages removed from nixpkgs-unstable

**Issue:** [#4](https://github.com/denhamparry/template/issues/4)
**Status:** Complete
**Date:** 2026-07-02

## Problem Statement

Entering the nix dev shell (`nix develop` or `direnv allow`) fails to
evaluate:

```text
error: nodePackages has been removed. Many packages are now available at the
top level (e.g. `pkgs.package-name`). Check on https://search.nixos.org to
see if the package is still available.
```

### Current Behavior

- `flake.nix` tracks `nixpkgs-unstable` (`flake.nix:5`) and references
  `pkgs.nodePackages.prettier` (`flake.nix:30`).
- A freshly generated `flake.lock` pins nixpkgs revision `e8273b2`
  (2026-07-01), where the `nodePackages` attribute set has been removed.
- Anyone creating a new project from this template gets a broken dev shell
  immediately — the flake fails to evaluate, so `nix develop`, `direnv
  allow`, and the advisory `nix.yml` CI workflow all fail.
- Repos with an older committed `flake.lock` are unaffected until they
  update.

### Expected Behavior

- `nix develop` evaluates and enters the dev shell with all tools available,
  including `prettier`.
- `nix flake check` passes against current `nixpkgs-unstable`.

## Current State Analysis

### Relevant Code/Config

- `flake.nix:30` — `pkgs.nodePackages.prettier` is the only reference to the
  removed `nodePackages` attribute set in the repository (verified with
  `rg -n "nodePackages"`).
- All other packages in the dev shell (`pre-commit`, `nodejs`,
  `markdownlint-cli`, `shellcheck`, `gitleaks`, `gh`) already use top-level
  attributes and are unaffected.
- `flake.lock` is **not** tracked in git. The issue #2 plan
  (`docs/plan/issues/2_align_template_with_fleet_conventions.md:244`)
  records the deliberate decision: "No `flake.lock` committed — first
  `nix develop` generates it per-project."

### Related Context

- Upstream nixpkgs removed the `nodePackages` scope; `prettier` is now a
  top-level attribute (`pkgs.prettier`), confirmed on
  <https://search.nixos.org>.
- The `~/.claude` config repo has the same `flake.nix` pattern and needs the
  same fix — out of scope for this PR (separate repository), tracked as a
  follow-up note.
- The `main` worktree currently has a staged, uncommitted `flake.lock` (the
  bug reproduction artifact). Per the issue #2 decision above, it should not
  be committed; it is left for the user to discard.

## Solution Design

### Approach

Replace the removed `nodePackages.prettier` reference with the top-level
`prettier` attribute. This is the upstream-recommended migration (the error
message itself points to top-level packages) and the minimal change that
restores evaluation.

### Implementation

```diff
-            pkgs.nodePackages.prettier
+            pkgs.prettier
```

### Benefits

- Dev shell evaluates again on current `nixpkgs-unstable`.
- New projects created from the template work out of the box.
- No behaviour change: the same prettier CLI lands on `PATH`.

## Implementation Plan

### Step 1: Fix the prettier package reference

**File:** `flake.nix`

**Changes:**

- Line 30: replace `pkgs.nodePackages.prettier` with `pkgs.prettier`.
- No other changes — the surrounding comment ("Standalone CLI tools") still
  applies.

**Testing:**

```bash
nix flake check
```

### Step 2: Verify the dev shell provides prettier

**File:** none (verification only)

**Changes:** none.

**Testing:**

```bash
nix develop --command prettier --version
```

Expected: a prettier version string prints; no evaluation error. The
transient `flake.lock` generated by this command must **not** be committed
(per the issue #2 decision) — remove it after verification if created.

## Testing Strategy

### Unit Testing

Not applicable — this is a one-line nix expression change with no project
code.

### Integration Testing

**Test Case 1: Flake evaluates**

1. Run `nix flake check` in the repo root.
2. Expected: no `nodePackages has been removed` error; check passes.

**Test Case 2: Dev shell provides all tools**

1. Run `nix develop --command prettier --version`.
2. Expected: prints the prettier version from the top-level package.

### Regression Testing

- Confirm the other dev shell tools still resolve: the `shellHook` prints
  versions for pre-commit, node, prettier, markdownlint, shellcheck,
  gitleaks, and gh on shell entry.
- Confirm no `flake.lock` is accidentally committed (`git status` clean of
  lock files before commit).

## Success Criteria

- [x] `flake.nix` references `pkgs.prettier` instead of
  `pkgs.nodePackages.prettier`
- [x] `nix flake check` passes against current `nixpkgs-unstable`
- [x] `nix develop --command prettier --version` succeeds (prettier 3.8.3)
- [x] No `flake.lock` committed (per issue #2 template decision)
- [x] Pre-commit hooks pass

## Files Modified

1. `flake.nix` - Replace `pkgs.nodePackages.prettier` with `pkgs.prettier`
   (line 30)

## Related Issues and Tasks

### Depends On

- None

### Blocks

- Any new project created from this template that wants a working nix dev
  shell

### Related

- Issue #2 / plan `docs/plan/issues/2_align_template_with_fleet_conventions.md`
  — introduced `flake.nix` and documented the no-committed-lock decision

### Enables

- Working `nix develop` / direnv flow for template consumers

## References

- [GitHub Issue #4](https://github.com/denhamparry/template/issues/4)
- [nixpkgs package search](https://search.nixos.org) — `prettier` top-level
  attribute
- `docs/plan/issues/2_align_template_with_fleet_conventions.md` — flake
  design decisions

## Notes

### Key Insights

- The template intentionally ships no `flake.lock`, so template consumers
  always evaluate against latest `nixpkgs-unstable`. That makes the template
  sensitive to upstream attribute removals like this one — the trade-off was
  accepted in issue #2 for per-project freshness.
- The staged-but-uncommitted `flake.lock` on the `main` worktree is a
  reproduction artifact and should be discarded, not committed.

### Alternative Approaches Considered

1. **Commit a pinned `flake.lock`** — Would shield consumers from upstream
   breakage but reverses the issue #2 decision and goes stale in a template
   context. Not chosen ❌
2. **Pin nixpkgs to a release branch (e.g. `nixos-25.11`)** — More stable,
   but the fleet convention is `nixpkgs-unstable` and this widens the change
   beyond the bug. Not chosen ❌
3. **Replace `nodePackages.prettier` with top-level `prettier`** — Minimal,
   upstream-recommended migration. Chosen ✅

### Best Practices

- When nixpkgs removes an attribute scope, prefer the top-level attribute
  the error message points to and verify on <https://search.nixos.org>.
- The advisory `nix.yml` workflow runs on flake-file PRs, so CI gives a
  second verification of this change.

## Plan Review

**Reviewer:** Claude Code (workflow-research-plan)
**Review Date:** 2026-07-02
**Original Plan Date:** 2026-07-02

### Review Summary

- **Overall Assessment:** Approved
- **Confidence Level:** High
- **Recommendation:** Proceed to implementation

### Strengths

- Root cause independently confirmed: evaluating
  `nodePackages.prettier` against the pinned revision
  `e8273b29fe1390ec8d4603f2477357555291432e` (the `e8273b2` from the issue)
  hits the nixpkgs alias-removal path exactly as the issue describes.
- Fix independently confirmed: `prettier` evaluates at top level in that
  same revision (`prettier.version` → `3.8.3`) under
  `legacyPackages.aarch64-darwin`, so the package is available on this
  platform.
- The plan correctly identifies `flake.nix:30` as the **only**
  `nodePackages` reference in the repo (verified with `rg`).
- The claim that all other dev shell packages are unaffected was verified:
  `pre-commit`, `nodejs`, `markdownlint-cli`, `shellcheck`, `gitleaks`, and
  `gh` all evaluate in the pinned revision.
- The no-committed-`flake.lock` decision is correctly sourced from the
  issue #2 plan rather than assumed, and the plan explicitly guards against
  accidentally committing the transient lock generated during verification.
- Scope is exactly right: one line, no drive-by changes.

### Gaps Identified

1. **Gap 1:** `nix flake check` may not exercise the devShell output on all
   nix versions unless `--all-systems` semantics apply; devShells are
   checked for the current system, which is sufficient here.
   - **Impact:** Low
   - **Recommendation:** Keep both verification commands (`nix flake check`
     and `nix develop --command prettier --version`) as planned — the
     `nix develop` invocation is the authoritative proof.

### Edge Cases Not Covered

1. **Edge Case 1:** A consumer with an old cached `flake.lock` in an
   existing project won't see this fix until they update their lock.
   - **Current Plan:** Notes this in the problem statement (older locks are
     unaffected until updated); no action needed in the template.
   - **Recommendation:** None — out of the template's control.

### Alternatives Assessed During Review

1. **Commit a pinned `flake.lock`:** already considered and correctly
   rejected in the plan — it reverses the documented issue #2 decision and
   goes stale in a template.
2. **Pin nixpkgs to a stable release branch:** correctly rejected as
   out-of-scope for a one-line bug fix and against fleet convention.

### Risks and Concerns

1. **Risk 1:** Future upstream attribute removals will break the template
   the same way, since no lock is committed.
   - **Likelihood:** Medium (nixpkgs-unstable churns)
   - **Impact:** Low (advisory `nix.yml` CI surfaces it early)
   - **Mitigation:** Accepted trade-off per issue #2; the weekly/advisory
     CI catches it. Not a blocker for this fix.

### Required Changes

None — the plan can be implemented as written.

### Optional Improvements

- [ ] Apply the same fix to the `~/.claude` config repo's `flake.nix`
  (separate repository, noted in the issue) — track as a follow-up outside
  this PR.

### Verification Checklist

- [x] Solution addresses root cause identified in GitHub issue
- [x] All acceptance criteria from issue are covered
- [x] Implementation steps are specific and actionable
- [x] File paths and code references are accurate
- [x] Security implications considered and addressed (none — nix package
  reference only)
- [x] Performance impact assessed (none)
- [x] Test strategy covers critical paths and edge cases
- [x] Documentation updates planned (none needed — no docs reference
  `nodePackages`)
- [x] Related issues/dependencies identified (issue #2 lock decision)
- [x] Breaking changes documented (none)
